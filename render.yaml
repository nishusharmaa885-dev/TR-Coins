services:
  - type: web
    name: aiohttp-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m aiohttp.web -H 0.0.0.0 -P $PORT app:app
    envVars:
      - key: PORT
        value: 10000
